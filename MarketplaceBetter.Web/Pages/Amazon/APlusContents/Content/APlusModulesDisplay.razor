@using MarketplaceBetter.Domain.Model.Amazon.APlusContents;

@if (Content.Modules.Any())
{
    <MudLink Href="@Link" title="Click to see" Style="text-decoration: none;">@Content.Modules.Count modules</MudLink>
}

@code {
    [Parameter] public APlusContentModel Content { get; set; }

    private string Link;

    protected override void OnInitialized()
    {
        base.OnInitialized();

        if (Content.Modules.Any())
        {
            Link = $"a-plus-modules?ContentName={ Content.Name }";
        }
    }
}
