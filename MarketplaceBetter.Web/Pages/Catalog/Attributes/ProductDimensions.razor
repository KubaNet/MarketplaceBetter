@using MarketplaceBetter.Domain.Entities.Base;
@using MarketplaceBetter.Domain.Model.Base;
@using MarketplaceBetter.Domain.Model.Catalog.Attributes;
@using MarketplaceBetter.Services.Domain.Base.Interfaces;
@using MarketplaceBetter.Services.Domain.Catalog.Attributes.Interfaces;
@using MarketplaceBetter.Services.Model;

@page "/product-dimensions"

@inject NavigationManager Navigation
@inject ISnackbar Snackbar
@inject IDialogService DialogService
@inject IProductDimensionsService DimensionsService
@inject IUserService UserService

<MudText Typo="Typo.h4" Class="mt-2">Product Dimensions</MudText>

<MainBody>
    <MudTable T="ProductDimensionsModel" @ref="Table" ServerData="@(new Func<TableState, Task<TableData<ProductDimensionsModel>>>(Reload))" Dense="true" Hover="true" RowsPerPage=CurrentRowsPerPageValue RowsPerPageChanged="OnRowsPerPageChanged">
        <ToolBarContent>
            <MudTextField Value="ListRequest.SearchString" Placeholder="Search" T="string" ValueChanged="Filter" Immediate="true" Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
            <MudSpacer />
            <MudButton OnClick="Add" Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Add" Color="Color.Primary" Class="ml-2">Add Product Dimensions</MudButton>
        </ToolBarContent>
        <HeaderContent>
            <MudTh><MudTableSortLabel SortLabel="id" T="ProductDimensionsModel">Id</MudTableSortLabel></MudTh>
            <MudTh><MudTableSortLabel SortLabel="product" T="ProductDimensionsModel">Product</MudTableSortLabel></MudTh>
            <MudTh><MudTableSortLabel SortLabel="size" T="ProductDimensionsModel">Size</MudTableSortLabel></MudTh>
            @if (ShowEUMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="weight_g" T="ProductDimensionsModel">Weight [g]</MudTableSortLabel></MudTh>
            }
            @if (ShowUSMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="weight_lb" T="ProductDimensionsModel">Weight [lb]</MudTableSortLabel></MudTh>
            }
            @if (ShowEUMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="depth_cm" T="ProductDimensionsModel">Depth [cm]</MudTableSortLabel></MudTh>
            }
            @if (ShowUSMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="depth_in" T="ProductDimensionsModel">Depth [in]</MudTableSortLabel></MudTh>
            }
            @if (ShowEUMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="width_cm" T="ProductDimensionsModel">Width [cm]</MudTableSortLabel></MudTh>
            }
            @if (ShowUSMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="width_in" T="ProductDimensionsModel">Width [in]</MudTableSortLabel></MudTh>
            }
            @if (ShowEUMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="height_cm" T="ProductDimensionsModel">Height [cm]</MudTableSortLabel></MudTh>
            }
            @if (ShowUSMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="height_in" T="ProductDimensionsModel">Height [in]</MudTableSortLabel></MudTh>
            }
            @if (ShowEUMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="length_cm" T="ProductDimensionsModel">Length [cm]</MudTableSortLabel></MudTh>
            }
            @if (ShowUSMetrics)
            {
                <MudTh><MudTableSortLabel SortLabel="length_in" T="ProductDimensionsModel">Length [in]</MudTableSortLabel></MudTh>
            }
            <MudTh Style="width: 170px;">Actions</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd>@context.Id</MudTd>
            <MudTd DataLabel="Product">@context.Product.Code</MudTd>
            <MudTd DataLabel="Size">@context.Size.Name</MudTd>
            @if (ShowEUMetrics)
            {
                <MudTd DataLabel="Weight [g]">@context.WeightInGrams.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowUSMetrics)
            {
                <MudTd DataLabel="Weight [lb]">@context.WeightInPounds.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowEUMetrics)
            {
                <MudTd DataLabel="Depth [cm]">@context.DepthInCentimeters.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowUSMetrics)
            {
                <MudTd DataLabel="Depth [in]">@context.DepthInInches.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowEUMetrics)
            {
                <MudTd DataLabel="Width [cm]">@context.WidthInCentimeters.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowUSMetrics)
            {
                <MudTd DataLabel="Width [in]">@context.WidthInInches.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowEUMetrics)
            {
                <MudTd DataLabel="Height [cm]">@context.HeightInCentimeters.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowUSMetrics)
            {
                <MudTd DataLabel="Height [in]">@context.HeightInInches.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowEUMetrics)
            {
                <MudTd DataLabel="Length [cm]">@context.LengthInCentimeters.ToTwoDecimalDigits()</MudTd>
            }
            @if (ShowUSMetrics)
            {
                <MudTd DataLabel="Length [in]">@context.LengthInInches.ToTwoDecimalDigits()</MudTd>
            }
            <MudTd>
                <MudButton Variant="Variant.Outlined" Size="Size.Small" Color="Color.Default" OnClick="@(() => AddSimilar(context.Id))">Add</MudButton>
                <MudButton Variant="Variant.Outlined" Size="Size.Small" Color="Color.Default" OnClick="@(() => Edit(context.Id))">Edit</MudButton>
            </MudTd>
        </RowTemplate>
        <PagerContent>
            @if (!ShowAll.HasValue || ShowAll == false)
            {
                <MudTablePager PageSizeOptions="new int[] { Standard.SmallestRowsPerPage, Standard.MediumRowsPerPage, Standard.LargestRowsPerPage }" />
            }
        </PagerContent>
    </MudTable>
</MainBody>

@code {
    [Parameter][SupplyParameterFromQuery] public string SearchString { get; set; }
    [Parameter][SupplyParameterFromQuery] public long? ProductId { get; set; }
    [Parameter][SupplyParameterFromQuery] public bool? ShowAll { get; set; }

    private MudTable<ProductDimensionsModel> Table;
    private ListRequest ListRequest = new();

    private int CurrentRowsPerPageValue = Standard.InitialRowsPerPage;
    private bool ShowUSMetrics;
    private bool ShowEUMetrics;

    protected override void OnInitialized()
    {
        base.OnInitialized();

        ProcessQueryParameters();
        DecideMetrics();
    }

    private void DecideMetrics()
    {
        InstanceModel currentInstance = UserService.GetCurrentInstance();

        ShowUSMetrics = currentInstance.SystemName == InstanceEnum.US || currentInstance.SystemName == InstanceEnum.All;
        ShowEUMetrics = currentInstance.SystemName != InstanceEnum.US;
    }

    private void ProcessQueryParameters()
    {
        if (!String.IsNullOrWhiteSpace(SearchString))
        {
            ListRequest.SearchString = SearchString;
        }
        if (ProductId.HasValue)
        {
            ListRequest.SearchString = $"product_id: {ProductId.Value}";
        }
        if (ShowAll.HasValue)
        {
            ListRequest.ShowAll = ShowAll.Value;
        }
    }

    private Task<TableData<ProductDimensionsModel>> Reload(TableState state)
    {
        state.PopulateListRequest(ListRequest);

        IList<ProductDimensionsModel> dimensions = DimensionsService.GetForListRequest(ListRequest);

        return Task.FromResult(new TableData<ProductDimensionsModel>() { TotalItems = DimensionsService.CountForListRequest(ListRequest), Items = dimensions });
    }

    private void OnRowsPerPageChanged(int rowsPerPage)
    {
        CurrentRowsPerPageValue = rowsPerPage;
    }

    private void Filter(string searchString)
    {
        ListRequest.SearchString = searchString;

        string newUrl = Navigation.GetUriWithQueryParameter(Standard.SearchQueryParamater, String.IsNullOrWhiteSpace(searchString) ? null : searchString);
        Navigation.NavigateTo(newUrl, replace: true);

        Table.ReloadServerData();

        StateHasChanged();
    }

    private async void Add()
    {
        var result = await DialogService.Show<ProductDimensionsDialog>(null, new DialogParameters { ["Title"] = "Add Product Dimensions" }).Result;

        ProcessDialogResult(result);
    }

    private async void AddSimilar(long id)
    {
        var result = await DialogService.Show<ProductDimensionsDialog>(null, new DialogParameters { ["Title"] = "Add Product Dimensions", ["AddingSimilar"] = true, ["DimensionsId"] = id }).Result;

        ProcessDialogResult(result);
    }

    private async void Edit(long id)
    {
        var result = await DialogService.Show<ProductDimensionsDialog>(null, new DialogParameters { ["Title"] = "Edit Product Dimensions", ["IsEdit"] = true, ["DimensionsId"] = id }).Result;

        ProcessDialogResult(result);
    }

    private void ProcessDialogResult(DialogResult result)
    {
        if (!result.Canceled)
        {
            Table.ReloadServerData();
        }
    }
}
