@using MarketplaceBetter.Domain.Model.Catalog.Products
@using MarketplaceBetter.Services.Domain.Catalog.Products.Interfaces

@page "/brands"
<PageTitle>Brands</PageTitle>

@inject IBrandService BrandService
@inject IDialogService DialogService

<MudText Typo="Typo.h4" Class="mt-2">Brands</MudText>

<MainBody>
    <MudTable Items="@BrandsData" Dense="true" Hover="true" RowsPerPage=Standard.InitialRowsPerPage>
        <ToolBarContent>
            <MudSpacer />
            <MudButton OnClick="Add" Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Add" Color="Color.Primary">Add Brand</MudButton>
        </ToolBarContent>
        <HeaderContent>
            <MudTh>Id</MudTh>
            <MudTh>Name</MudTh>
            <MudTh>Code</MudTh>
            <MudTh Style="width: 100px;">Actions</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Id">@context.Id</MudTd>
            <MudTd DataLabel="Name">@context.Name</MudTd>
            <MudTd DataLabel="Code">@context.Code</MudTd>
            <MudTd><MudButton Variant="Variant.Outlined" Size="Size.Small" OnClick="@(() => Edit(context.Id))">Edit</MudButton></MudTd>
        </RowTemplate>
        <PagerContent>
            <MudTablePager PageSizeOptions="new int[] { Standard.SmallestRowsPerPage, Standard.MediumRowsPerPage, Standard.LargestRowsPerPage }" />
        </PagerContent>
    </MudTable>
</MainBody>

@code {
    private IList<BrandModel> BrandsData { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();

        BrandsData = BrandService.GetAll();
    }

    private async void Add()
    {
        DialogResult result = await DialogService.Show<BrandDialog>(null, new DialogParameters { ["Title"] = "Add Brand" }).Result;

        ProcessDialogResult(result);
    }

    private async void Edit(long id)
    {
        DialogResult result = await DialogService.Show<BrandDialog>(null, new DialogParameters { ["Title"] = "Edit Brand", ["IsEdit"] = true, ["BrandId"] = id }).Result;

        ProcessDialogResult(result);
    }

    private void ProcessDialogResult(DialogResult result)
    {
        if (!result.Canceled)
        {
            BrandsData = BrandService.GetAll();

            StateHasChanged();
        }
    }
}
