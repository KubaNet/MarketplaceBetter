@using MarketplaceBetter.Domain.Entities.Amazon.Campaigns
@using MarketplaceBetter.Domain.Model.Amazon.Campaigns
@using MarketplaceBetter.Services.Domain.Amazon.Campaigns.Interfaces

@inject IProductAdService ProductAdService

<MudSwitch T="bool" Checked="Checked" Color="Color.Tertiary" CheckedChanged="OnCheckedChanged" />

@code {
    [Parameter] public ProductAdModel ProductAd { get; set; }

    private bool Checked = false;

    protected override void OnInitialized()
    {
        base.OnInitialized();

        if (ProductAd.Status.SystemName == AdEntityStatusEnum.Enabled)
        {
            Checked = true;
        }
    }

    private void OnCheckedChanged(bool status)
    {
        Checked = status;

        if (status == true)
        {
            ProductAdService.UpdateStatus(ProductAd.Id, AdEntityStatusEnum.Enabled);
        }
        else
        {
            ProductAdService.UpdateStatus(ProductAd.Id, AdEntityStatusEnum.Paused);
        }
    }
}
