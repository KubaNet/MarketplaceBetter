@using MarketplaceBetter.Domain.Entities.Amazon.Campaigns
@using MarketplaceBetter.Domain.Model.Amazon.Campaigns
@using MarketplaceBetter.Services.Domain.Amazon.Campaigns.Interfaces

@inject INegativeKeywordService NegativeKeywordService

<MudSwitch T="bool" Checked="Checked" Color="Color.Tertiary" CheckedChanged="OnCheckedChanged" />

@code {
    [Parameter] public NegativeKeywordModel NegativeKeyword { get; set; }

    private bool Checked = false;

    protected override void OnInitialized()
    {
        base.OnInitialized();

        if (NegativeKeyword.Status.SystemName == AdEntityStatusEnum.Enabled)
        {
            Checked = true;
        }
    }

    private void OnCheckedChanged(bool status)
    {
        Checked = status;

        if (status == true)
        {
            NegativeKeywordService.UpdateStatus(NegativeKeyword.Id, AdEntityStatusEnum.Enabled);
        }
        else
        {
            NegativeKeywordService.UpdateStatus(NegativeKeyword.Id, AdEntityStatusEnum.Paused);
        }
    }
}
